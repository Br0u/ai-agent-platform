# Skills extension boundary

本计划只交付 Skill 库+审核；Agent 仍不加载任何 Skill。已发布 revision 只是审核通过的不可变制品，不是运行时注册或启用。

`LocalSkills` 或其他运行时 attachment 属于下一计划。未来集成只有在显式 catalog 注册、授权、隔离和验收后才可用；本目录和 Registry 中的 `published` 状态都不能证明 Skill 已对 Agent 生效。
